function switchTab(tabId,btn){document.querySelectorAll('.itinerary-panel').forEach(panel=>{panel.classList.add('hidden');});const selectedPanel=document.getElementById('panel-'+tabId);if(selectedPanel){selectedPanel.classList.remove('hidden');}document.querySelectorAll('.itinerary-tab-btn').forEach(button=>{button.classList.remove('active-tab');});btn.classList.add('active-tab');}function toggleFaq(btn){const item=btn.parentElement;const isOpen=item.classList.contains('open');document.querySelectorAll('.faq-item').forEach(i=>{i.classList.remove('open');});if(!isOpen){item.classList.add('open');}}const reveal=()=>{const reveals=document.querySelectorAll('.reveal');const windowHeight=window.innerHeight;const elementVisible=150;reveals.forEach(el=>{const elementTop=el.getBoundingClientRect().top;if(elementTop<windowHeight-elementVisible){el.classList.add('active');}});};window.addEventListener('scroll',reveal);window.addEventListener('load',reveal);(function(){const canvas=document.getElementById('hero-canvas');if(!canvas)return;function syncSize(){const parent=canvas.parentElement;const w=parent.clientWidth;const h=parent.clientHeight;if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h;}}if(typeof ResizeObserver!=='undefined'){new ResizeObserver(syncSize).observe(canvas.parentElement);}syncSize();const gl=canvas.getContext('webgl')||canvas.getContext('experimental-webgl');if(!gl)return;const vs=` attribute vec2 a_position;varying vec2 v_texCoord;void main(){v_texCoord=a_position*0.5+0.5;gl_Position=vec4(a_position,0.0,1.0);}`;const fs=` precision highp float;varying vec2 v_texCoord;uniform float u_time;uniform vec2 u_resolution;void main(){vec2 uv=v_texCoord;vec3 green=vec3(0.49,0.45,0.67);//Primary(#7c72ab)vec3 lime=vec3(0.90,1.0,0.78);//Secondary(#e6ffc7)vec3 purple=vec3(0.20,0.20,0.20);//Tertiary/Dark Violet(#333333)float noise=sin(uv.x*2.5+u_time*0.4)*0.5+0.5;noise+=cos(uv.y*1.5-u_time*0.2)*0.5+0.5;noise*=0.5;vec3 color=mix(green,purple,noise);color=mix(color,lime,pow(noise,4.0)*0.3);color*=(1.2-uv.y*0.4);gl_FragColor=vec4(color,1.0);}`;function cs(type,src){const s=gl.createShader(type);gl.shaderSource(s,src);gl.compileShader(s);return s;}const prog=gl.createProgram();gl.attachShader(prog,cs(gl.VERTEX_SHADER,vs));gl.attachShader(prog,cs(gl.FRAGMENT_SHADER,fs));gl.linkProgram(prog);if(!gl.getProgramParameter(prog,gl.LINK_STATUS)){console.warn("Shader program failed to link");return;}gl.useProgram(prog);const buf=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buf);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array([-1,-1,1,-1,-1,1,1,1]),gl.STATIC_DRAW);const pos=gl.getAttribLocation(prog,'a_position');gl.enableVertexAttribArray(pos);gl.vertexAttribPointer(pos,2,gl.FLOAT,false,0,0);const uTime=gl.getUniformLocation(prog,'u_time');const uRes=gl.getUniformLocation(prog,'u_resolution');function render(t){syncSize();gl.viewport(0,0,canvas.width,canvas.height);if(uTime)gl.uniform1f(uTime,t*0.001);if(uRes)gl.uniform2f(uRes,canvas.width,canvas.height);gl.drawArrays(gl.TRIANGLE_STRIP,0,4);requestAnimationFrame(render);}render(0);})();(function(){const form=document.getElementById('contact-form');const sink=document.getElementById('wpforms-sink');if(!form||!sink)return;const btn=document.getElementById('form-submit-btn');const note=document.getElementById('form-note');const success=document.getElementById('form-success');const pageUrl=document.getElementById('form-page-url');if(pageUrl)pageUrl.value=window.location.href;let submitting=false;form.addEventListener('submit',function(){if(!form.checkValidity())return;submitting=true;if(btn){btn.disabled=true;btn.textContent='Enviando...';}if(note)note.textContent='Enviando tus datos...';});sink.addEventListener('load',function(){if(!submitting)return;submitting=false;form.hidden=true;if(success)success.hidden=false;});})();let currentLightboxIndex=0;const totalGalleryImages=18;function openLightbox(index){currentLightboxIndex=index;const modal=document.getElementById('gallery-lightbox');const img=document.getElementById('lightbox-img');if(modal&&img){img.src=`assets/galeria/galeria${currentLightboxIndex+1}.jpg`;modal.classList.add('active');document.body.style.overflow='hidden';}}function closeLightbox(e){if(e&&e.target&&e.target.id!=='gallery-lightbox')return;closeLightboxForce();}function closeLightboxForce(){const modal=document.getElementById('gallery-lightbox');if(modal){modal.classList.remove('active');document.body.style.overflow='';}}function changeLightboxImg(dir,e){if(e)e.stopPropagation();currentLightboxIndex=(currentLightboxIndex+dir+totalGalleryImages)%totalGalleryImages;const img=document.getElementById('lightbox-img');if(img){img.src=`assets/galeria/galeria${currentLightboxIndex+1}.jpg`;}}document.addEventListener('keydown',function(e){const modal=document.getElementById('gallery-lightbox');if(modal&&modal.classList.contains('active')){if(e.key==='Escape')closeLightboxForce();if(e.key==='ArrowLeft')changeLightboxImg(-1);if(e.key==='ArrowRight')changeLightboxImg(1);}});function scrollCarousel(trackId,amount){const track=document.getElementById(trackId);if(!track)return;const wrapper=track.parentElement;if(wrapper){wrapper.scrollBy({left:amount,behavior:'smooth'});}}